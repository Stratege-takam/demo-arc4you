<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DemoPCBE99925.ManageCourse.WPF.Common;component/ResourceDictionary/Constants.xaml" />
    </ResourceDictionary.MergedDictionaries>
    
    
    <!-- Default style of Image-->
    <Style x:Key="DefaultImageStyle" TargetType="{x:Type Image}">
        <Setter Property="MaxWidth" Value="{StaticResource ImageDefaultWidth}" />
        <Setter Property="MaxHeight" Value="{StaticResource ImageDefaultHeight}" />
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="Margin" Value="{StaticResource TextControlDefaultMargin}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Stretch" Value="Uniform" />
    </Style>
    
</ResourceDictionary>